# 环境配置与使用指南

这份文档面向人，不讲 AI 的执行细节，只回答两个问题：环境怎么准备，这套东西怎么用。上手方式有两套，任选其一即可。

## 方法一：让 Agent 分析 Skill，手把手教你（推荐）

不需要通读任何文档，把判断工作交给 AI：

1. 把本仓库 clone 到本地，将 `mc-design`、`mc-build`、`mc-modify` 三个目录复制进你所用 Agent 的技能目录（以 WorkBuddy 为例是 `~/.workbuddy/skills/`）。
2. 直接对 Agent 说："读一下 mc-design 和 mc-build 的 SKILL.md，教我怎么把环境配好，然后带我跑通第一个建筑。"
3. Agent 会自己阅读 SKILL.md 里的流程和触发词，逐步引导你：检查缺哪些依赖、游戏怎么开、MCP 服务怎么配、怎么触发第一次设计与建造。中途任何一步报错，把报错原样发给它即可。

适合人群：不想读文档，希望 AI 全程带着走、边做边学。

## 方法二：自己动手，按下面的清单配置

### 第 1 步 准备游戏

- 安装 Minecraft Java 版（已在 1.21.1 完整验证，其余版本理论上可行但未测试）；
- 创建或进入一个单人世界，选择"对局域网开放"，并勾选允许作弊；
- 记住你要让 AI 操控的游戏角色名（下文以 XiaoBuddy 为例），游戏保持在这个角色登录在线的状态。

### 第 2 步 安装 MCP 引擎（连接游戏的桥梁）

- 确认已安装 Node.js（版本不低于 20.10.0）；
- 克隆并构建 [minecraft-mcp-server-blueprint](https://github.com/ChenZeLongKnight/minecraft-mcp-server-blueprint)：

```bash
git clone https://github.com/ChenZeLongKnight/minecraft-mcp-server-blueprint.git
cd minecraft-mcp-server-blueprint
npm install
npm run build
```

- 在你的 MCP 客户端（WorkBuddy、Claude Desktop 等）中添加服务器配置：

```json
{
  "mcpServers": {
    "minecraft": {
      "command": "node",
      "args": [
        "<本仓库路径>/dist/main.js",
        "--host", "localhost",
        "--port", "25565",
        "--username", "XiaoBuddy"
      ]
    }
  }
}
```

### 第 3 步 安装浏览器自动化 MCP（网页设计器用）

设计阶段使用的网页体素设计器依赖浏览器自动化来打开与操控页面，安装 [browser-use](https://github.com/browser-use/browser-use) 提供的 MCP 并在客户端中启用。

### 第 4 步 安装技能

```bash
git clone https://github.com/ChenZeLongKnight/Ai-build-for-minecraft.git
cp -r Ai-build-for-minecraft/mc-design \
      Ai-build-for-minecraft/mc-build \
      Ai-build-for-minecraft/mc-modify \
      ~/.workbuddy/skills/
```

### 第 5 步（可选）图片还原路线的额外依赖

只有想把照片、像素图转成建筑时才需要：

- Python 3.10 以上，安装四个包：`numpy`、`Pillow`、`scipy`、`onnxruntime`；
- 显著性分割模型权重，获取方式见 [`models/README.md`](models/README.md)，仓库内附轻量版开箱可用。

### 验证配置是否成功

在 MCP 客户端的工具列表中能看到 `build-blueprint`、`verify-region`、`list-schematics` 等蓝图工具，且游戏角色可以被移动，即为配置成功。

## 路径不需要你操心

整个流程里与路径有关的事只有一件：蓝图引擎装在了哪里。这件事也不用你设置任何环境变量——第一次使用时 agent 会自动探测（优先环境变量，其次默认的 `D:/mcserver`），都找不到才问你要一次；之后本会话所有脚本调用、目录创建全部由 agent 完成，输出目录缺失时脚本会自动创建。你需要做的只有回答 agent 的问题。

## 日常怎么用

三个技能各管一段，对 Agent 说对应的话就会自动触发：

| 你想做什么 | 对 Agent 说 | 触发的技能 |
|-----------|------------|-----------|
| 从零设计一个建筑，或把照片转成建筑 | "帮我设计蓝图"、"把这张照片做成建筑" | mc-design |
| 把设计好的蓝图盖出来 | "建造"、"落地"、"秒落" | mc-build |
| 对已建好的建筑小改 | "改一下"、"换个材质"、"拆掉重摆" | mc-modify |

典型的完整流程：设计产出蓝图 → 一句话建造 → 自己在游戏里按 F2 截图看观感 → 不满意就对 Agent 说改哪里 → 差量微调。观感是否满意由你说了算，Agent 负责保证方块层面与蓝图一致。
