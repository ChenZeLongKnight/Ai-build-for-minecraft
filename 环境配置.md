# 环境配置与使用指南

本文面向第一次接触本项目的使用者，说明需要准备哪些环境、如何完成配置，以及配置完成后如何开始使用。整套流程以 AI Agent 为主导，需要人工完成的事情很少，大多数检查和设置都由 agent 自动处理。

## 技能的组成与使用顺序

本套件包含四个技能，各司其职。mc-env 负责环境的配置与体检，mc-design 负责建筑的体素设计，mc-build 负责把设计结果建造到游戏里，mc-modify 负责对建成的建筑做小幅修改。

第一次使用时请严格按照下面的顺序进行：先安装技能，再运行 mc-env 完成环境配置，最后才使用其余三个技能。环境没有配置好之前，其余技能无法正常工作。

## 第一步，安装技能

将本仓库克隆到本地，把其中的四个技能目录复制到 agent 的技能目录中。以 WorkBuddy 为例，技能目录位于用户主目录下的 .workbuddy/skills。

```bash
git clone https://github.com/ChenZeLongKnight/Ai-build-for-minecraft.git
cp -r Ai-build-for-minecraft/mc-env \
      Ai-build-for-minecraft/mc-design \
      Ai-build-for-minecraft/mc-build \
      Ai-build-for-minecraft/mc-modify \
      ~/.workbuddy/skills/
```

## 第二步，运行环境配置

技能安装完成后，对 agent 说一句配置环境，即可触发 mc-env。agent 会逐项检查运行条件，包括 Node.js 版本、Minecraft 游戏状态、蓝图引擎、MCP 连接、浏览器自动化工具，以及图片还原功能所需的 Python 依赖。

检查过程中，凡是 agent 能自动完成的事项都会直接完成，例如创建缺失的目录、确定引擎路径。你最多只需要回答一个问题，即蓝图引擎安装在哪里。检查结束后，agent 会输出一份体检报告，逐项说明哪些就绪、哪些缺失、缺失的部分如何补齐。

## 手动配置清单

如果你希望自己动手了解每个环节，可以按照下面的清单逐项准备。这一步做完之后，仍然建议运行一次 mc-env 做整体确认。

一、游戏环境。安装 Minecraft Java 版，本套件在 1.21.1 版本上完整验证过。进入单人世界后，将世界对局域网开放，并勾选允许作弊。记住你要让 agent 操控的游戏角色名。

二、Node.js。版本不低于 20.10.0，用于运行蓝图引擎和技能脚本。

三、蓝图引擎。它是 agent 与游戏之间的桥梁，来自另一个仓库，需要克隆并构建。

```bash
git clone https://github.com/ChenZeLongKnight/minecraft-mcp-server-blueprint.git
cd minecraft-mcp-server-blueprint
npm install
npm run build
```

构建完成后，在 agent 所用客户端的 MCP 配置中添加一个名为 minecraft 的服务器，指向构建产物。配置示例如下，其中角色名替换为你自己的游戏角色。

```json
{
  "mcpServers": {
    "minecraft": {
      "command": "node",
      "args": [
        "<引擎目录>/dist/main.js",
        "--host", "localhost",
        "--port", "25565",
        "--username", "XiaoBuddy"
      ]
    }
  }
}
```

四、浏览器自动化。安装 browser-use 提供的 MCP 并在客户端中启用。自主设计路线需要通过浏览器操控网页体素设计器，缺少这一项时设计功能无法使用，建造和修改不受影响。

五、图片还原依赖。这一项是可选的，只有想把照片或像素图转换成建筑时才需要。准备 Python 3.10 以上版本，安装 numpy、Pillow、scipy、onnxruntime 四个包，再按照 models 目录中的说明获取模型权重。

## 如何判断配置已经完成

在客户端的 MCP 工具列表中能看到 build-blueprint、verify-region、list-schematics 这几个工具，并且 agent 能够移动游戏中的角色，就说明环境已经就绪。用 mc-env 做过体检并通过的，可以直接开始使用。

## 日常使用

环境就绪之后，使用方式就是对 agent 说话。三个技能按照设计、建造、修改的顺序衔接，你说出意图，agent 自动调用对应的技能。

| 你想做的事情 | 可以这样说 | 触发的技能 |
| --- | --- | --- |
| 从零设计一个建筑，或者把照片转成建筑 | 帮我设计蓝图，把这张照片做成建筑 | mc-design |
| 把设计好的蓝图建造出来 | 建造，落地 | mc-build |
| 对已经建好的建筑做小幅调整 | 改一下，换个材质 | mc-modify |

一个完整的例子：先让 agent 设计蓝图，设计完成后说一句建造，agent 会把蓝图写入世界；然后你自己在游戏里按 F2 截图查看效果，满意就结束，不满意就告诉 agent 哪里要改，agent 通过差量修改完成调整。建筑的观感由你判断，方块层面与蓝图一致由 agent 保证。
